# Build artifacts
# Note: bin/ includes:
#   - bin/*.* (native binaries for local development)
#   - bin/linux-arm64/*.* (Linux ARM64 binaries for Docker)
#   - bin/linux-amd64/*.* (Linux x86_64 binaries for Docker/servers)
bin/
*.test
*.out
/matcher
/validator
/registry
/mock-rootlayer
/simple-agent

# Runtime data
data/
*-data/
*-raft/
val*-data/
val*-raft/
raft-data/
*.pid
*.sock

# Logs
*.log
e2e-test-logs/
validation-consensus-test-logs/
simple-consensus-logs/
subnet-logs/

# Environment and secrets
.env
.env.local
.env.*.local
*.pem
*.key
*_key
*_private_key
credentials.json
secrets/

# Production configuration files (keep templates only)
config/subnet.yaml
config/validator-*.yaml
config/blockchain.yaml
!config/*.template

# Subnet info files (contain subnet IDs and contract addresses)
subnet-info-*.txt
subnets/

# IDE and OS
.gocache
.DS_Store
.vscode/
.idea/

# Documentation (temporary/working files)
CLAUDE.md
AGENTs.md

# Test and development scripts (temporary)
check-vb-status.sh
test-*.sh
start-validators-fixed.sh
matcher-e2e.yaml
new-subnet-validators.txt

# Node modules (if any)
node_modules/
cspell.json
package-lock.json
package.json

# Temporary validation bundle test docs
analysis-reports/
cometbft-validators/